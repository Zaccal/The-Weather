import { API_TOKEN } from '@/root'

export const ApiConfig = {
  baseUrl: 'http://api.weatherapi.com/v1/',
  searchUrl: 'search.json',
  currentUrl: 'current.json',
  forecastUrl: 'forecast.json',
  key: API_TOKEN
}
